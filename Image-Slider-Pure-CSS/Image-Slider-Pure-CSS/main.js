// const slideshowItem = document.querySelectorAll('.slideshow-item');
// let initialTime = 0;

// const animateFunction = (slide, initialTime) => {
//   slide.style.animationDelay = `${initialTime}s`;
//   slide.getElementsByTagName('img').animationDelay = `${initialTime}s`;
// };

// slideshowItem.forEach((slide, initialTime) => {
//   slide.style.animationDelay = `${initialTime}s`;
//   slide.getElementsByTagName('img').animationDelay = `${initialTime}s`;
//   initialTime += 5;
//   console.log(initialTime);
// });
